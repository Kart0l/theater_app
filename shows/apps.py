"""AppConfig for the shows app."""

from django.apps import AppConfig


class ShowsConfig(AppConfig):
    """App config for shows app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "shows"

"""AppConfig for the halls app."""

from django.apps import AppConfig


class HallsConfig(AppConfig):
    """App config for halls app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "halls"

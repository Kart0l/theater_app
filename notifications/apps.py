"""AppConfig for the notifications app."""

from django.apps import AppConfig


class NotificationsConfig(AppConfig):
    """App config for notifications app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "notifications"
